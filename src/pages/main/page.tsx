import { Button } from "@/shared/ui/button";

export const MainPage = () => {
    return (
        <main className="flex-auto">
            <Button>Get started with interactive form</Button>
            <Button variant="secondary">Quick start (Premium feature)</Button>
        </main>
    );
};
